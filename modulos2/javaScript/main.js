// define todas las funciones
import {suma } from './suma'
import {resta } from './resta'
import {muntiplicacion } from './muntiplicacion'
import {Division } from './divicion'
import {modulo } from './modulo'

const
    nun1 = 9;
    nun2 = 3;
    
let result = suma( nun1, nun2)
    console.log (  `la resta es  ${result}`);

    result = resta( nun1, nun2)
    console.log (  `la resta es  ${result}`);

    result = muntiplicacion( nun1, nun2)
    console.log (  `la resta es  ${result}`);

    result = Division( nun1, nun2)
    console.log (  `la resta es  ${result}`);

    result = modulo( nun1, nun2)
    console.log (  `la resta es  ${result}`);









``